/* import Card from "./Card";

function Modal(shoes) {
  return (
    <div className="row">
      {shoes.map((a, i) => {
        return <Card key={i} shoes={shoes[i]} i={i} />;
      })}
    </div>
  );
}

export default Modal; */
